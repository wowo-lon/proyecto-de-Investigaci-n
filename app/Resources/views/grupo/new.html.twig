{% extends 'layout.html.twig' %}

{% block contenido %}
    <h1>Grupo de Investigación Nuevo</h1>

    <div class="col-xs-12 col-sm-6 col-md-5 col-lg-4">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            
            <input type="submit" value="Crear"  class="btn btn-success"/>
            <a href="{{ path('grupo_index') }}" class="btn btn-danger">Volver</a>

        {{ form_end(form) }}
    </div>
    <script>
    	var usuarios;
    	$(document).ready(function(){
    		var select = $("grupo_lider");
    		var options;
    		{% for usuario in users %}
    		    options = options + '<option value="{{ usuario.id }}">{{ usuario.nombreCompleto }}</option>'; 
    		    console.log({{ usuario.id }});
    		{% endfor %}

    		$("#grupo_lider option").remove();
	        $("#grupo_lider").append(options);
	        
		});
    </script>
{% endblock %}
