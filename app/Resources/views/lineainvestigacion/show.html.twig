{% extends '::layout.html.twig' %}

{% block contenido %}
    <h1>Linea de Investigacion</h1>

    <div class="col-xs-12 col-sm-10 col-md-10 col-lg-10">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ lineaInvestigacion.id }}</td>
                </tr>
                <tr>
                    <th>Nombre</th>
                    <td>{{ lineaInvestigacion.nombre }}</td>
                </tr>
            </tbody>
        </table>
        <a href="{{ path('lineainvestigacion_index') }}" class="btn btn-info">volver a lista</a>
        <a href="{{ path('lineainvestigacion_edit', { 'id': lineaInvestigacion.id }) }}" class="btn btn-success">Editar</a>
        
        {{ form_start(delete_form) }}
            <input type="submit" value="Eliminar" class="btn btn-danger">
        {{ form_end(delete_form) }}
    </div>
    
{% endblock %}
