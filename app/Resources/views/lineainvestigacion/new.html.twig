{% extends '::layout.html.twig' %}

{% block contenido %}
    <h1>Nueva Linea de Investigacion</h1>

    <div class="col-xs-12 col-sm-6 col-md-4 col-lg-4">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Crear" class="btn btn-success"/>
            <a href="{{ path('lineainvestigacion_index') }}" class="btn btn-danger">Volver</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
