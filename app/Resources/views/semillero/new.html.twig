{% extends 'layout.html.twig' %}

{% block contenido %}
    <h1>Nuevo Semillero de Investigación</h1>

    <div class="col-xs-12 col-sm-12 col-md-8 col-lg-8">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Crear" class="btn btn-success"/>
            <a href="{{ path('semillero_index') }}" class="btn btn-danger">Volver</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
